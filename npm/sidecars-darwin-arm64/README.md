# `@imagemin-rs/sidecars-darwin-arm64`

Prebuilt image codec sidecars for `imagemin-rs` on macOS ARM64.

This package is installed automatically as an optional platform dependency. It currently contains
the pinned cwebp 1.6.0 executable, its provenance manifest, and complete third-party license texts.
